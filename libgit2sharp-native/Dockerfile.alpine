FROM alpine:3.19

# Install build dependencies
RUN apk add --no-cache \
    build-base \
    cmake \
    ninja \
    git \
    pkgconfig \
    openssl-dev \
    linux-headers

WORKDIR /src

COPY . .

# Make the build script executable
RUN chmod +x /src/libgit2sharp-native/build-native-linux.sh

ENTRYPOINT ["/src/libgit2sharp-native/build-native-linux.sh"]
