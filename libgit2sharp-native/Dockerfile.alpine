FROM alpine:3.19

# Install build dependencies
RUN apk add --no-cache \
    build-base \
    cmake \
    ninja \
    git \
    pkgconfig \
    openssl-dev \
    linux-headers \
    bash

WORKDIR /src

COPY . .

ENTRYPOINT ["/src/libgit2sharp-native/build-native-linux.sh"]
